# Source Origin

The product implementation in this handoff comes from the user-provided Grok 4.6 `Forklight.zip`.

This packaging operation intentionally did **not** clean or rewrite product source code.

It changed governance only:

- archived old Grok generated specs/agent instructions;
- installed `AGENTS.md`;
- installed the locked v1.2 docs and Codex staged prompts.

Codex must independently run the baseline before deleting runtime/template infrastructure.
